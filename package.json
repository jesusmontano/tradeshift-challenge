{
  "name": "tradeshift-challenge",
  "version": "0.1.0",
  "homepage": "http://jesusmontano.github.io/tradeshift-challenge",
  "private": true,
  "dependencies": {
    "@tradeshift/elements": "^0.5.3",
    "@tradeshift/elements.button": "^0.5.3",
    "@tradeshift/elements.header": "^0.5.3",
    "@tradeshift/elements.icon": "^0.5.3",
    "@tradeshift/elements.note": "^0.5.3",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "5.0.1",
    "react-tradeshift-ui": "^2.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^2.1.1"
  }
}
